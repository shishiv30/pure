// Minimal entry file for presentation slider page
// This page is self-contained and doesn't need the main app initialization

export default (function (win) {
	// Presentation slider is fully self-contained in index.html
	// No additional initialization needed
})(window);
