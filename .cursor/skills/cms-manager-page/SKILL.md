---
name: cms-manager-page
description: Alias for cms-manager. Use cms-manager for page create/update and header links.
---

> **Alias of `cms-manager`.** Use `.cursor/skills/cms-manager/SKILL.md` for the full workflow.
Do not duplicate workflow steps here; keep this file as a pointer-only alias.

Summary: Use **cms-manager** for creating pages, updating page data/meta, and header nav. API: `POST /api/pages`, `PUT /api/pages/by-name/:name`. See `cms/API.md`.
