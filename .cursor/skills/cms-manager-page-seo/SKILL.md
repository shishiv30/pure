---
name: cms-manager-page-seo
description: Alias for cms-manager. Use cms-manager for page meta (title, desc, keywords) and SEO.
---

> **Alias of `cms-manager`.** Use `.cursor/skills/cms-manager/SKILL.md` for the full workflow.
Do not duplicate workflow steps here; keep this file as a pointer-only alias.

Summary: Use **cms-manager** for updating page meta (title, desc, keywords, path) for SEO. API: `PUT /api/pages/by-name/<pageName>` with body `{ "meta": { "title": "...", "desc": "...", "keywords": "..." } }`. See `cms/API.md`.
